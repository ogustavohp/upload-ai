/** @type {import('next').NextConfig} */
const nextConfig = {
  // webpack: (config) => {
  //   config.module.rules.push({
  //     test: /ffmpeg\.wasm$/,
  //     type: 'javascript/auto',
  //   })
  //   return config
  // },
}

module.exports = nextConfig
